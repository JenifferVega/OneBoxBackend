# ==============================================================================
# onebox-agent-py/requirements.txt
# ==============================================================================
# Dependencias del agente OneBox
# ==============================================================================

# AWS SDK para Bedrock
boto3>=1.34.0

# HTTP requests para las APIs de Gmail
requests>=2.31.0

# Para desarrollo local (FastAPI)
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0

# Twilio para WhatsApp y SMS
twilio>=9.0.0

# Opcional: para debugging
python-dotenv>=1.0.0

# Parseo de documentos adjuntos
pypdf>=4.0.0
python-docx>=1.1.0
python-multipart>=0.0.9

# numpy: pin a una versión con wheels para Lambda Python 3.11.
# numpy 2.4.x solo tiene wheels nuevos que requieren toolchain meson + gcc no
# presentes en la imagen Lambda → falla compilando from source. numpy 2.1
# es la última con wheels manylinux_2_28 universales para esta imagen.
# langchain-aws acepta numpy<3,>=1.0, así que esta pin es compatible.
numpy>=1.26,<2.2

# Grafo del agente (LangGraph + providers LangChain)
langgraph>=1.0,<2
langchain-core>=1.4,<2
langchain-aws>=1.5,<2         # ChatBedrockConverse (provider por defecto en prod)
langchain-anthropic>=1.4,<2   # API directa de Anthropic (fallback)
# langchain-google-genai NO se incluye: su última versión exige langchain-core<1.0
# (incompatible con langgraph 1.x). El provider 'gemini' del llm_factory hace
# lazy-import y da error claro si se selecciona sin instalar el paquete.